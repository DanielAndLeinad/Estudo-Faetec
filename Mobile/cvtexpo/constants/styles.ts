import { StyleSheet } from 'react-native';
//Exportando um objeto com as propriedades de estilo
const styles = StyleSheet.create({
    centralizado: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
});

export default styles;